# Shared requirements for ALL python components (data-pipeline + rag-query).
# One virtualenv for the whole repo keeps setup to a single "pip install -r requirements.txt".

# Database drivers
psycopg2-binary==2.9.10
pgvector==0.3.6
neo4j==5.26.0

# Config
python-dotenv==1.0.1

# HTTP calls to model APIs and to the AI News JSON API (hand-written, no heavy SDK)
requests==2.32.3

# BM25 keyword search (pure Python implementation, easy to read the algorithm itself)
rank-bm25==0.2.2

# PCA for the embedding-space visualization demo (hand-written SVD-based PCA, numpy just supplies the SVD)
numpy==2.4.6
