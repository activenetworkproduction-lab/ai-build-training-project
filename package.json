{
  "name": "project040-training",
  "private": true,
  "scripts": {
    "dev:ocr:server": "pnpm --filter ocr-server start:dev",
    "dev:ocr:web": "pnpm --filter ocr-web dev",
    "build": "pnpm -r build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "esbuild"
    ]
  }
}
